language: java

jdk:
    - oraclejdk8

install:
    - pip install --user codecov

before_install:
    - pip install --user -U pip
    
after_success:
    - codecov

notifications:
    email:
        - builds@fitbur.com
    on-success: change
    on-failure: always
