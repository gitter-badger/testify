language: java

jdk:
    - oraclejdk8

before_install:
    - pip install --upgrade pip
    - pip install --user codecov

after_success:
    - codecov

notifications:
    email:
        - builds@fitbur.com
    on-success: change
    on-failure: always
    
before_install:
  - pip install codecov
after_success:
  - codecov
